apiVersion: apps/v1
kind: Deployment
metadata:
  name: mtool-backend
spec:
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: mtool-backend
          image: wbitt/network-multitool
          ports:
            - containerPort: 80
          env:
            - name: HTTP_PORT
              value: "80"
